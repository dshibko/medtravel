<div class="">
    <h1 class="page-header">Клиенты</h1>
    <div class="table-responsive">
        <?php if (!empty($this->clients)) : ?>
            <table class="table table-hover table-striped table-bordered">
                <thead>
                <tr>
                    <th>ФИО</th>
                    <th>Статус</th>
                    <th>Услуга</th>
                    <th>След. заплан. контакт</th>
                    <th>Дата обращения</th>
                    <th>Страна</th>
                    <th>Клиника</th>
                    <th>Врач</th>
                    <th>Менеджер</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                    <?php foreach ($this->clients as $client) : ?>
                        <tr style="background-color: <?=$client['color']?>">
                            <td><?=$client['fio']; ?></td>
                            <td><?=$client['status']; ?></td>
                            <td><?=$client['service']['name']; ?></td>
                            <td><?=$client['contactType']; ?></td>
                            <td><?=$client['dos']->format('d-M-Y'); ?></td>
                            <td><?=$client['country']; ?></td>
                            <td><?=$client['clinic']['name']; ?></td>
                            <td><?=$client['doctor']['name']; ?></td>
                            <td><?=$client['manager']['displayName']; ?></td>
                            <td style="width: 40px;">
                                <a href="/clients/edit/<?=$client['id']?>"><img width="15px;" src="/img/edit.png"></a>
                                <a href="/clients/edit/<?=$client['id']?>?view=1"><img width="15px;" src="/img/view.png"></a>
                                <!--<br>
                                <?php /*if ($this->getCurrentUser()->getId() != $user->getId()) : */?>
                                    <a href="/users/delete/<?/*=$user->getId()*/?>" onClick="return confirm('Подтвердите удаление пользователя')">
                                        <span class="label label-danger">Удалить</span>
                                    </a>
                                --><?php /*endif; */?>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        <?php else : echo 'Нет клиентов<br>'; endif; ?>
        <a href="/clients/add">
            <button type="button" class="btn btn-lg btn-primary">Добавить клиента</button>
        </a>
    </div>
</div>